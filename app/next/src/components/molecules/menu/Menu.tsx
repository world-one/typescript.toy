import React, { FC } from 'react';
import MenuItem from '../../atoms/menu-item/MenuItem';

const Menu: FC = () => {
  return (
    <nav>
      <ul>
        <MenuItem />
      </ul>
    </nav>
  );
};

export default Menu;
